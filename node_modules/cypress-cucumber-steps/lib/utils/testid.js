"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTestIdElements = getTestIdElements;
var options_1 = require("./options");
/**
 * Get test ID elements.
 *
 * @param testId - Test ID.
 * @param selector - Pseudo selector.
 * @returns - Cypress element.
 * @private
 */
function getTestIdElements(testId, selector, options) {
    var queries = [
        "[data-testid=".concat(JSON.stringify(testId), "]"),
        "[data-test-id=".concat(JSON.stringify(testId), "]"),
    ].map(function (query) { return (selector ? "".concat(query, ":").concat(selector) : query); });
    return cy.get(queries.join(','), (0, options_1.getOptions)(options));
}
//# sourceMappingURL=testid.js.map